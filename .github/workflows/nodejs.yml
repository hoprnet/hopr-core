name: Node.js CI

on:
  pull_request:
    branches: [master, develop]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.9.1]
        yarn-version: [1.19.2]
        repository: ['core', 'chat', 'protos', 'server']

    steps:
      # Checkout, Cache, Filter, Setup Envs and Steps
      - name: Checkout repository
        uses: actions/checkout@v2
      - uses: dorny/paths-filter@v2.2.0
        id: filter
        with:
          filters: |
            core:
              - '**/*'
              - '!chat'
              - '!protos'
              - '!server'
            chat:
              - 'chat/**/*'
            protos:
              - 'protos/**/*'
            server:
              - 'server/**/*'
      - id: set-pkgPath
        run: |
          echo "::set-output name=pkgPath::${{ matrix.repository }}/package.json"
      - id: set-workDir
        if: steps.filter.outputs.core == 'true'
        run: |
          echo "::set-output name=workDir::/"
      - id: set-workDir
        if: steps.filter.outputs.core != 'true'
        run: |
          echo "::set-output name=workDir::${{ matrix.repository }}"
      - name: Cache node_modules
        id: cache-modules
        uses: actions/cache@v1
        with:
          path: node_modules
          key: ${{ runner.OS }}-build-${{ hashFiles(steps.set-pkgPath.pkgPath) }}
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      # Building and testing library
      - name: Install node modules
        working-directory: ${{ steps.set-workDir.workDir }}
        if: steps.cache-modules.outputs.cache-hit != 'true' && steps.filter.outputs.${{ matrix.repository }} == 'true'
        run: |
          yarn policies set-version ${{ matrix.yarn-version }}
          yarn install --frozen-lockfile
      - name: Build library
        working-directory: ${{ steps.set-workDir.workDir }}
        if: steps.filter.outputs.${{ matrix.repository }} == 'true'
        run: |
          yarn policies set-version ${{ matrix.yarn-version }}
          yarn build
      - name: Test library
        working-directory: ${{ steps.set-workDir.workDir }}
        if: steps.filter.outputs.${{ matrix.repository }} == 'true'
        run: |
          yarn policies set-version ${{ matrix.yarn-version }}
          yarn test
